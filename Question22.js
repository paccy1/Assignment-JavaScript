function displayMultiplesOfFive(start, end) {
    for (let i = start; i <= end; i += 5) {
        console.log(i);
    }
}

// Call the function to display multiples of 5 between 1 and 100
displayMultiplesOfFive(1, 100);